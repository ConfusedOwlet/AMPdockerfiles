FROM cubecoders/ampbase

RUN apt-get update && apt-get -y upgrade && \
	apt-get install -y python3-pip python3-setuptools python3-venv pipx && \
	apt-get clean && \
	rm -rf /var/lib/apt/lists/*
RUN pipx install protontricks

ENTRYPOINT ["/ampstart.sh"]
CMD []
